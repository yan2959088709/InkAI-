# InkAI - AI小说创作系统

> 基于大语言模型的中长篇小说自动创作系统，支持上千章小说的智能生成

---

## 🎯 一句话介绍

**InkAI是一个能让AI写出上千章长篇小说的智能创作系统。它不是简单的续写工具，而是包含故事线生成、质量评估、节奏控制的完整工作流。**

---

## 📊 核心亮点

```
InkAI 核心能力

智能工作流：
├── 标签选择 → 人物创建 → 故事线生成 → 章节写作 → 质量评估
└── 全自动，用户只需输入需求

中长篇优化：
├── 3章节奏循环（缓冲→转折→强推）
├── 双层故事线咬合（明线+暗线）
├── 伏笔分层管理（短/中/长）
└── 剧情阶梯升级（个人→局部→全局）

上千章支持：
├── 分卷管理（每40章1卷）
├── 批量续写（10/50/100章）
├── 断点续写（随时停止继续）
└── 自动卷摘要生成

不掉拍机制：
├── 节奏控制（RhythmKeeper）
├── 知识同步（UnifiedKnowledgeSync）
├── 卷间关联（VolumeConnection）
└── 人物弧光追踪

专家优化：
├── 节奏规则配置化（支持6种小说类型）
├── 容错机制（自动重试+降级章节）
└── 双线咬合自然化（检查而非强制）
```

---

## 🏗️ 系统架构

### 整体架构

```
┌─────────────────────────────────────────────────────────────┐
│                      用户界面层                              │
├─────────────────────────────────────────────────────────────┤
│  Web API (Flask)  │  CLI命令行  │  Python API               │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│                      工作流控制层                            │
├─────────────────────────────────────────────────────────────┤
│                  InkAIWorkflowOptimized                     │
│  ┌──────────────────────────────────────────────────────┐  │
│  │ 标签选择 → 人物创建 → 故事线生成 → 章节写作 → 质量评估 │  │
│  └──────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│                      智能体层（27个）                        │
├─────────────────────────────────────────────────────────────┤
│  核心智能体 (6个)         │  续写智能体 (9个)                │
│  ├─ TagSelectorAgent      │  ├─ ContinuationStorylineGenerator│
│  ├─ CharacterCreatorAgent │  ├─ ContinuationChapterWriter     │
│  ├─ StorylineGenerator    │  ├─ ContinuationQualityAssessor   │
│  ├─ ChapterWriterAgent    │  └─ 6个专项评估/改进Agent          │
│  ├─ QualityAssessorAgent  │                                   │
│  └─ CharacterImprover     │                                   │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│                      知识管理层                              │
├─────────────────────────────────────────────────────────────┤
│  双层知识库                        │  不掉拍机制              │
│  ├─ CoreKnowledgeManager          │  ├─ RhythmKeeper         │
│  ├─ DynamicKnowledgeManager       │  ├─ RhythmConfig         │
│  ├─ IntelligentContextSelector    │  ├─ FaultTolerance       │
│  └─ VectorDatabase                │  └─ UnifiedKnowledgeSync │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│                      数据存储层                              │
├─────────────────────────────────────────────────────────────┤
│  文件系统存储                                                 │
│  ├─ novels/{novel_id}/chapter_XXX.json    (章节内容)        │
│  ├─ novels/{novel_id}/characters.json     (人物档案)        │
│  ├─ novels/{novel_id}/storyline.json      (故事线)          │
│  ├─ novels/{novel_id}/volume_XXX.json     (卷摘要)          │
│  └─ novels/{novel_id}/dynamic_knowledge.json (动态知识)     │
└─────────────────────────────────────────────────────────────┘
```

### 智能体系统

```
智能体架构（27个）

核心智能体（6个）
├── TagSelectorAgent - 标签选择
├── CharacterCreatorAgent - 人物创建
├── StorylineGeneratorAgent - 故事线生成
├── ChapterWriterAgent - 章节写作
├── QualityAssessorAgent - 质量评估
└── CharacterImprover - 人物改进

续写智能体（3个）
├── ContinuationStorylineGenerator - 续写故事线（嵌入节奏配置）
├── ContinuationChapterWriter - 续写章节
└── ContinuationQualityAssessor - 续写评估

专项智能体（12个）
├── 6个评估Agent
│   ├── ContinuationCharacterConsistencyAssessor
│   ├── ContinuationPlotLogicAssessor
│   ├── ContinuationWorldConsistencyAssessor
│   ├── ContinuationStyleConsistencyAssessor
│   ├── ContinuationReaderExperienceAssessor
│   └── ContinuationLongTermConsistencyAssessor
└── 6个改进Agent（对应6个评估）

辅助智能体（2个）
├── ChapterSummaryGenerator - 章节摘要生成
└── EnhancedCharacterAnalyzer - 角色分析

基础类（2个）
├── BaseContinuationAssessor - 评估基础类
└── BaseContinuationImprover - 改进基础类
```

---

## 📋 完整业务流程

### 一、新书创作流程

```
用户输入需求
    ↓
┌─────────────────────────────────────────────────────────────┐
│ 步骤1: 创建小说项目                                          │
│ 输入: 用户需求 + 小说标题                                     │
│ 输出: 小说ID                                                 │
└─────────────────────────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────────────────────────┐
│ 步骤2: 标签选择                                              │
│ 输入: 用户需求                                               │
│ 处理: LLM分析，生成4类标签                                    │
│ 输出: 类型/主题/风格/受众标签                                 │
└─────────────────────────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────────────────────────┐
│ 步骤3: 人物创建                                              │
│ 输入: 用户需求 + 标签                                         │
│ 处理: LLM生成主角 + 配角档案                                  │
│ 输出: 人物档案（性格/背景/动机/关系）                         │
└─────────────────────────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────────────────────────┐
│ 步骤4: 故事线生成                                            │
│ 输入: 用户需求 + 标签 + 人物                                  │
│ 处理: LLM生成整体故事线                                      │
│ 输出: 世界观/核心冲突/阶段规划/双线设定                       │
└─────────────────────────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────────────────────────┐
│ 步骤5: 写第1章                                               │
│ 输入: 故事线 + 人物 + 世界观                                  │
│ 处理: ChapterWriterAgent生成                                 │
│ 输出: 3000-5000字章节                                        │
└─────────────────────────────────────────────────────────────┘
```

### 二、续写流程（核心）

```
┌─────────────────────────────────────────────────────────────┐
│                    单章续写流程                              │
└─────────────────────────────────────────────────────────────┘

第1步: 获取节奏配置（RhythmConfig）
├── 根据小说类型加载配置（爽文/悬疑/都市/言情/玄幻/历史）
├── 确定章节类型（缓冲章/转折章/强推章）
└── 确定节奏速度和冲突强度

第2步: 检查双线交汇（DualStorylineChecker）
├── 检查是否是检查点（每N章检查一次）
├── 分析明线/暗线元素
├── 生成咬合建议（非强制）
└── 根据剧情阶段调整咬合强度

第3步: 生成故事线
├── 输入:
│   ├── 上一章结尾（2500字）
│   ├── 前3章摘要
│   ├── 卷摘要（新卷时）
│   ├── 人物档案
│   ├── 世界观设定
│   ├── 节奏配置
│   └── 双线咬合建议
├── 处理:
│   ├── 确定剧情阶段（开端/中段/结局）
│   ├── 确定双线比重（明线/暗线）
│   └── LLM生成下一章故事线
└── 输出: 章节标题/情节要点/关键事件/伏笔/钩子

第4步: 质量评估
├── 5维度评估:
│   ├── 情节逻辑性
│   ├── 人物一致性
│   ├── 世界观一致性
│   ├── 语言风格
│   └── 读者体验
└── 输出: 评分 + 改进建议

第5步: 章节写作
├── 输入:
│   ├── 故事线
│   ├── 节奏指导
│   ├── 双线咬合要求
│   └── 上一章结尾
├── 处理:
│   ├── LLM生成3000-5000字
│   ├── 遵循黄金结构
│   └── 遵循章节类型要求
└── 输出: 章节正文

第6步: 保存章节
├── 保存JSON文件
├── 保存TXT文件
└── 更新元数据

第7步: 知识库同步
├── 更新动态知识库
├── 更新知识图谱
├── 同步两者数据
└── 验证一致性
```

### 三、批量续写流程（带容错）

```
用户输入: 续写100章
    ↓
┌─────────────────────────────────────────────────────────────┐
│ 批量续写循环（带容错机制）                                   │
│                                                             │
│ for chapter in range(100):                                  │
│   ├── 获取节奏配置（RhythmConfig）                          │
│   ├── 检查双线交汇（DualStorylineChecker）                  │
│   ├── 生成故事线                                            │
│   ├── 质量评估                                              │
│   ├── 章节写作                                              │
│   ├── 保存章节                                              │
│   ├── 更新知识库                                            │
│   ├── 如果是卷末：生成卷摘要                                │
│   └── 保存进度                                              │
│                                                             │
│ 容错机制：                                                  │
│   ├── 失败自动重试（最多3次）                               │
│   ├── 重试失败生成降级章节                                  │
│   └── 断点续写（中断后继续）                                │
└─────────────────────────────────────────────────────────────┘
    ↓
输出: 100个章节 + 进度报告 + 卷摘要
```

### 四、分卷管理流程

```
1000章小说 = 25卷 × 40章

第1卷（第1-40章）
├── 卷开端（1-10章）：建立冲突
├── 卷中段（11-30章）：推进发展
└── 卷结尾（31-40章）：高潮收束
    ↓
生成第1卷摘要
    ↓
第2卷（第41-80章）
├── 回顾上卷关键事件
├── 延续人物发展
├── 推进未完成伏笔
└── 升级冲突赌注
    ↓
...

第25卷（第961-1000章）
├── 解决终极矛盾
├── 完成人物弧光
└── 全书闭环
```

---

## 🔧 特色功能详解

### 1. 节奏规则配置化（支持6种小说类型）

```python
# 不同类型的节奏配置
NOVEL_TYPE_CONFIGS = {
    "爽文": {
        "cycle_chapters": 2,  # 2章循环
        "volume_chapters": 50,  # 每卷50章
        "pacing": "fast"  # 快节奏
    },
    "悬疑": {
        "cycle_chapters": 4,  # 4章循环
        "volume_chapters": 30,  # 每卷30章
        "pacing": "slow"  # 慢节奏
    },
    "都市": {
        "cycle_chapters": 3,  # 3章循环
        "volume_chapters": 40,  # 每卷40章
        "pacing": "medium"  # 中等节奏
    },
    "言情": {
        "cycle_chapters": 2,  # 2章循环
        "volume_chapters": 35,  # 每卷35章
        "pacing": "slow"  # 慢节奏
    },
    "玄幻": {
        "cycle_chapters": 2,  # 2章循环
        "volume_chapters": 60,  # 每卷60章
        "pacing": "fast"  # 快节奏
    },
    "历史": {
        "cycle_chapters": 4,  # 4章循环
        "volume_chapters": 40,
        "pacing": "slow"  # 慢节奏
    }
}
```

### 2. 3章节奏循环（以都市为例）

```
缓冲铺垫章 → 升级转折章 → 强推进章 → 缓冲铺垫章 → ...
   (第1章)      (第2章)      (第3章)      (第4章)

每章类型要求：
┌─────────────────────────────────────────────────────────────┐
│ 缓冲铺垫章                                                  │
│ 冲突: 内部冲突/人际冲突                                      │
│ 重点: 人物成长、伏笔铺垫                                     │
│ 示例: 主角面对内心挣扎，队友发现秘密                          │
└─────────────────────────────────────────────────────────────┘
┌─────────────────────────────────────────────────────────────┐
│ 升级转折章                                                  │
│ 冲突: 多类型冲突叠加                                         │
│ 重点: 矛盾升级、双线交叉                                     │
│ 示例: 反派计划暴露，危机升级                                 │
└─────────────────────────────────────────────────────────────┘
┌─────────────────────────────────────────────────────────────┐
│ 强推进章                                                    │
│ 冲突: 外部强冲突                                             │
│ 重点: 核心主线推进、爽点高光                                 │
│ 示例: 主角潜入敌营，获取关键证据                             │
└─────────────────────────────────────────────────────────────┘
```

### 3. 双层故事线咬合（改进：检查而非强制）

```
明线（古玩鉴定冒险）          暗线（家族秘密）
       ↓                            ↓
  主角鉴定古玩    ←——咬合——→   十二年前真相
       ↓                            ↓
  揭露造假阴谋    ←——咬合——→   双玉扳指秘密
       ↓                            ↓
  对抗黑暗势力    ←——咬合——→   顾明渊身份
       ↓                            ↓
       └────────→ 汇合 ←───────────┘
                   ↓
              最终大高潮

咬合强度（根据剧情阶段）：
- 开端（25%）：弱咬合，暗线仅铺垫
- 中段（50%）：中咬合，有自然交汇则强化
- 结尾（25%）：强咬合，双线汇合

改进：不再强制"每3章必须交汇"
- 每3章检查是否有自然交汇点
- 有则强化，无则铺垫
- 避免"强行插入"导致剧情生硬
```

### 4. 伏笔分层管理

```
┌─────────────────────────────────────────────────────────────┐
│ 短伏笔（3章内回收）                                         │
│ 示例: 本章主角捡到钥匙，第3章用它打开密室                    │
│ 作用: 维持单章爽感                                          │
└─────────────────────────────────────────────────────────────┘
┌─────────────────────────────────────────────────────────────┐
│ 中伏笔（20章内回收）                                        │
│ 示例: 第5章埋下的反派习惯，第25章成为破局关键                │
│ 作用: 推动阶段高潮                                          │
└─────────────────────────────────────────────────────────────┘
┌─────────────────────────────────────────────────────────────┐
│ 长伏笔（全书回收）                                          │
│ 示例: 第1章母亲的遗言，第1000章成为破解阴谋的核心           │
│ 作用: 完成全书闭环                                          │
└─────────────────────────────────────────────────────────────┘
```

### 5. 容错机制

```
批量续写容错流程：

单章续写
    ↓
成功？ ──→ 是 ──→ 继续下一章
    ↓
    否
    ↓
自动重试（最多3次）
    ↓
成功？ ──→ 是 ──→ 继续下一章
    ↓
    否
    ↓
生成降级过渡章节
    ↓
标记为降级章节，继续下一章
```

### 6. 剧情阶梯升级

```
开端阶段（第1-250章，25%）
├── 冲突级别: 个人危机
├── 赌注范围: 个人安危
└── 主角状态: 异能觉醒

中段阶段（第251-750章，50%）
├── 冲突级别: 局部危机
├── 赌注范围: 团队生存
└── 主角状态: 能力提升

结局阶段（第751-1000章，25%）
├── 冲突级别: 全局危机
├── 赌注范围: 世界命运
└── 主角状态: 完成蜕变
```

---

## 📊 质量控制

### 单章质量检查清单

```
1. 是否有核心冲突？（主角目标 + 阻碍）
2. 开篇是否承接上一章？
3. 是否有情绪起伏？
4. 结尾是否有钩子？
5. 字数是否达标（3000-5000字）？
6. 是否符合章节类型要求？
7. 人物是否一致？
8. 伏笔是否推进？
```

### 多维度质量评估

| 维度 | 检查内容 |
|------|----------|
| 情节逻辑 | 因果关系、时间线、事件发展 |
| 人物一致 | 性格、行为、语言风格 |
| 世界观 | 设定一致性、规则遵循 |
| 语言风格 | 文笔质量、修辞手法 |
| 读者体验 | 流畅度、情感共鸣、悬念设置 |

---

## 🚀 使用方法

### 快速开始

```bash
# 方式1: 一键快速开始
python quick_start.py

# 方式2: 完整示例
python example_thousand_chapters.py

# 方式3: Web界面
python start_web.py
```

### 代码调用

```python
from core.batch_continuation import BatchContinuationManager

# 批量续写50章（自动应用所有优化）
manager = BatchContinuationManager()
result = manager.batch_continue("novel_id", num_chapters=50)

# 断点续写
result = manager.resume_batch("novel_id", remaining_chapters=30)
```

### API调用

```python
import requests

# 创建小说
response = requests.post("http://localhost:5000/api/novel/create", json={
    "title": "我的小说",
    "user_requirements": "都市异能小说..."
})

# 批量续写
response = requests.post("http://localhost:5000/api/novel/{id}/batch", json={
    "num_chapters": 50
})
```

---

## 📈 性能指标

| 指标 | 数值 |
|------|------|
| 单章生成时间 | 3-5分钟 |
| 单章字数 | 3000-5000字 |
| 每章LLM调用 | 3-4次 |
| 100章生成时间 | 5-8小时 |
| 1000章生成时间 | 50-80小时 |

---

## 🎨 创作标准

### 中长篇核心原则

1. **双线咬合**：明暗线必须同根共生、高潮同步
2. **阶梯升级**：矛盾从小到大，赌注层层提升
3. **人物弧光**：角色成长与剧情发展同步
4. **伏笔分层**：短中长伏笔合理搭配

### 单章黄金结构

| 模块 | 字数 | 要求 |
|------|------|------|
| 开篇承接 | 300-450字 | 承接上文、明确目标 |
| 冲突展开 | 1200-1500字 | 行动→反馈→新障碍 |
| 本章高潮 | 600-750字 | 核心爆点、情绪峰值 |
| 结尾钩子 | 300-450字 | 悬念/危机/疑问 |

---

## 📁 项目结构

```
InkAI/
├── main.py                          # CLI入口
├── app.py                           # Web API入口
├── inkai_workflow_optimized.py      # 主工作流
├── data_manager.py                  # 数据管理
├── workflow_context.py              # 上下文管理
├── base_agent.py                    # 智能体基类
├── config.py                        # 配置文件
│
├── agents/                          # 智能体（27个）
│   ├── tag_selector.py              # 标签选择
│   ├── character_creator.py         # 人物创建
│   ├── storyline_generator.py       # 故事线生成
│   ├── chapter_writer.py            # 章节写作
│   ├── quality_assessor.py          # 质量评估
│   ├── continuation_*.py            # 续写相关智能体
│   └── base_continuation_*.py       # 基础类
│
├── core/                            # 核心模块
│   ├── core_knowledge_manager.py    # 核心知识库
│   ├── dynamic_knowledge_manager.py # 动态知识库
│   ├── intelligent_context_selector.py # 上下文选择
│   ├── volume_manager.py            # 分卷管理
│   ├── rhythm_config.py             # 节奏配置（新）
│   ├── rhythm_controller.py         # 节奏控制
│   ├── rhythm_keeper.py             # 节奏保持
│   ├── dual_storyline_checker.py    # 双线检查（改进）
│   ├── foreshadowing_lifecycle_manager.py # 伏笔管理
│   ├── storyline_progression_planner.py # 剧情规划
│   ├── volume_connection_manager.py # 卷间关联
│   ├── unified_knowledge_sync.py    # 知识同步
│   ├── fault_tolerance.py           # 容错机制（新）
│   └── batch_continuation.py        # 批量续写（改进）
│
├── utils/                           # 工具
│   ├── json_fixer.py                # JSON修复
│   ├── logger.py                    # 日志系统
│   └── chapter_quality_analyzer.py  # 质量分析
│
├── data/                            # 数据
│   └── novels/                      # 小说数据
│       └── {novel_id}/
│           ├── chapter_001.json     # 章节
│           ├── characters.json      # 人物
│           ├── storyline.json       # 故事线
│           └── volume_001.json      # 卷摘要
│
└── 文档
    ├── README.md                    # 项目介绍
    ├── PROJECT_INTRODUCTION.md      # 简洁介绍
    ├── ARCHITECTURE.md              # 技术架构
    ├── USAGE_GUIDE.md               # 使用指南
    └── OPTIMIZATION_PLAN.md         # 优化计划
```

---

## 🔍 总结

InkAI 不是一个简单的"AI续写工具"，而是一个完整的**中长篇小说创作系统**：

| 特性 | 说明 |
|------|------|
| 智能工作流 | 从标签选择到章节写作的完整流程 |
| 中长篇优化 | 3章循环、双线咬合、伏笔分层 |
| 上千章支持 | 分卷管理、批量续写、断点续写 |
| 质量控制 | 5维度评估、黄金结构检查 |
| 不掉拍保障 | 节奏控制、知识同步、卷间关联 |
| 专家优化 | 节奏配置化、容错机制、双线自然化 |

**核心价值**：让AI能够创作出结构完整、节奏稳定、长线连贯的中长篇小说。

**快速介绍**：InkAI是一个能让AI写出上千章长篇小说的智能创作系统。它不是简单的续写工具，而是包含故事线生成、质量评估、节奏控制的完整工作流。支持3章节奏循环、双层故事线咬合、伏笔分层管理，确保1000章小说节奏稳定不掉拍。
